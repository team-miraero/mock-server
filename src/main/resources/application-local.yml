spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3307/mock?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: ${DB_USERNAME:mock}
    password: ${DB_PASSWORD:mock}

mock:
  oauth:
    client-secret: ${MOCK_OAUTH_CLIENT_SECRET:local-mock-client-secret}
