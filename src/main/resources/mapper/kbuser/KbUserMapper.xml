<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jejuro.miraero.mock_server.kbuser.mapper.KbUserMapper">

    <select id="findByEmail"
            resultType="org.jejuro.miraero.mock_server.kbuser.domain.KbUser">
        SELECT kb_user_id, name, birth_date, email, company_name, monthly_income
        FROM kb_user
        WHERE email = #{email}
    </select>

    <select id="findById"
            resultType="org.jejuro.miraero.mock_server.kbuser.domain.KbUser">
        SELECT kb_user_id, name, birth_date, email, company_name, monthly_income
        FROM kb_user
        WHERE kb_user_id = #{kbUserId}
    </select>
</mapper>
